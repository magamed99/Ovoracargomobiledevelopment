// Figma Make virtual module — values injected by the build system at runtime.
// This stub satisfies TypeScript; the real values come from the platform.
export declare const projectId: string;
export declare const publicAnonKey: string;
